[pytest]
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

markers =
    first: first set of tests
    bbc: test for bbc
    second: second set of tests
    only: test only

addopts = --clean-alluredir
          --alluredir allure-results